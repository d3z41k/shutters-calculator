{{template "base" .}}

{{define "main"}}
    <h2>Shutters calculator</h2>
    {{if .Categories}}
        <table>
            <tr>
                <th>ID</th>
                <th>Название</th>
                <th>Цена, &#8381;</th>
            </tr>
            {{range .Categories}}
            <tr>
                <td>{{.ID}}</td>
                <td>{{.Name}}</td>
                <td>{{.Price}}</td>
            </tr>
            {{end}}
        </table>
    {{else}}
        <p>There's nothing to see here... yet!</p>
    {{end}}
{{end}}